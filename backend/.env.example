# Database connection
DATABASE_URL="postgresql://speedtest:speedtest@localhost:5432/speedtest?schema=public"

# Server configuration
PORT=3001
HOST=0.0.0.0

# Speedtest scheduler (cron expression)
# Default: every 5 minutes
SPEEDTEST_CRON="*/5 * * * *"

# Run speedtest immediately on startup
RUN_ON_STARTUP=false

# CORS origin (use specific origin in production)
CORS_ORIGIN="*"
