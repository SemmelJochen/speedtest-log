# Speedtest Logger - Environment Configuration
# Kopiere diese Datei nach .env und passe die Werte an

# Database
DB_USER=speedtest
DB_PASSWORD=speedtest
DB_NAME=speedtest

# Ports
FRONTEND_PORT=3000
BACKEND_PORT=3001

# API URL (wie das Frontend die API erreicht - aus Sicht des Browsers)
# Wenn du auf einem Server mit Domain läufst, ändere dies zu deiner Domain
# Diese URL wird zur Build-Zeit ins Frontend einkompiliert
VITE_API_URL=http://localhost:3001

# Speedtest Konfiguration
# Cron-Expression für automatische Tests (Standard: alle 5 Minuten)
SPEEDTEST_CRON=*/5 * * * *

# Speedtest beim Start ausführen (true/false)
RUN_ON_STARTUP=true
